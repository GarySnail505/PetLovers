
  # PetLovers Distributed Manager Prototype

  This is a code bundle for PetLovers Distributed Manager Prototype. The original project is available at https://www.figma.com/design/A56qS1aszhKI5cIAdsvFSd/PetLovers-Distributed-Manager-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
